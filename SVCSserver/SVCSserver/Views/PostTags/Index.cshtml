@model IEnumerable<SVCSserver.Models.PostTags>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Post.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tag.content)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Post.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tag.content)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PostTagsID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PostTagsID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PostTagsID })
        </td>
    </tr>
}

</table>
